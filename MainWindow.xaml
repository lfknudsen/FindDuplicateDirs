<Window x:Class="FindDuplicateDirs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xml:lang="en-GB"
        mc:Ignorable="d"
        Title="DupDirFinder"
        MinWidth="348"
        MinHeight="480"
        Icon="Graphics/Icon.png"
        SizeToContent="WidthAndHeight">
    <Grid>
        <DockPanel LastChildFill="True">
            <DockPanel DockPanel.Dock="Top" LastChildFill="False">
                <Button DockPanel.Dock="Left" Click="SelectDirs_OnClick" ToolTip="Select directories to traverse."
                        Margin="2 2 2 0" Padding="6 3 6 3">
                    Add directories
                </Button>
                <Button DockPanel.Dock="Left" Click="Clear_OnClick" ToolTip="Clear current list of directories."
                        Margin="2 2 2 0" Padding="6 3 6 3">
                    Clear list
                </Button>
                <Button DockPanel.Dock="Left" Click="SetInitialDir_OnClick"
                        ToolTip="Set the initial directory when clicking 'Add directories'."
                        Margin="2 2 2 0" Padding="6 3 6 3">
                    Set initial directory
                </Button>
                <Button MinWidth="38px" DockPanel.Dock="Right" Click="Activate_OnClick"
                        ToolTip="Locate subdirectories which have the same name."
                        Margin="2 2 2 0" Padding="6 3 6 3" Content="Start"
                        x:Name="BtnStart"/>
            </DockPanel>
            <StatusBar DockPanel.Dock="Bottom">
                <DockPanel LastChildFill="True">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <StatusBarItem>Directories: </StatusBarItem>
                        <StatusBarItem Content="{Binding Source={StaticResource DirList}, Path=Count}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <StatusBarItem>Duplicate Pairs: </StatusBarItem>
                        <StatusBarItem Content="{Binding Source={StaticResource DuplicateList}, Path=Count}" />
                    </StackPanel>
                </DockPanel>
            </StatusBar>
            <ListView DockPanel.Dock="Left" Margin="2 3 2 0" ItemsSource="{Binding Source={StaticResource DirList}}">
                <ListView.Resources>
                    <ContextMenu x:Key="DirectoryContextMenu">
                        <MenuItem Header="Remove" Click="DirItem_OnRightClick" />
                    </ContextMenu>
                </ListView.Resources>
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="ContextMenu" Value="{StaticResource DirectoryContextMenu}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView
                        ColumnHeaderToolTip="Directories to be traversed to look for duplicate sub-directories.">
                        <GridViewColumn DisplayMemberBinding="{Binding Path=FullName}" Header="Path" />
                    </GridView>
                </ListView.View>
            </ListView>
            <Border />
            <ListView DockPanel.Dock="Right" Margin="2 3 2 0"
                      ItemsSource="{Binding Source={StaticResource DuplicateList}}">
                <ListView.Resources>
                    <ContextMenu x:Key="DirectoryContextMenu">
                        <MenuItem Header="Remove" Click="DupItem_OnRightClick" />
                    </ContextMenu>
                </ListView.Resources>
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="ContextMenu" Value="{StaticResource DirectoryContextMenu}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView
                        ColumnHeaderToolTip="Identically-named sub-directory pairs.">
                        <GridViewColumn DisplayMemberBinding="{Binding Path=Item1.FullName}" Header="Path 1" />
                        <GridViewColumn DisplayMemberBinding="{Binding Path=Item2.FullName}" Header="Path 2" />
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>
    </Grid>
</Window>